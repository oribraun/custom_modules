import { NgTimeToStringDirective } from './ng-time-to-string.directive';

describe('NgTimeToStringDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTimeToStringDirective();
    expect(directive).toBeTruthy();
  });
});
